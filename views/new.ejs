<div class="form-container">
  <h2>Add New Game</h2>
  <form action="/games" method="POST">
    <div class="form-group">
      <label for="title">Title:</label>
      <input
        type="text"
        id="title"
        name="title"
        required
        class="form-control"
      />
    </div>

    <div class="form-group">
      <label for="description">Description:</label>
      <textarea
        id="description"
        name="description"
        required
        class="form-control"
      ></textarea>
    </div>

    <div class="form-group">
      <label for="gameUrl">Game URL:</label>
      <input
        type="text"
        id="gameUrl"
        name="gameUrl"
        required
        class="form-control"
      />
      <small>The URL of your game (e.g. fly.pieter.com)</small>
    </div>

    <div class="form-group">
      <label for="xProfile">X (Twitter) Profile:</label>
      <input type="text" id="xProfile" name="xProfile" class="form-control" />
      <small>Optional: Your X (Twitter) handle (e.g. @username)</small>
    </div>

    <button type="submit" class="btn btn-primary">Add Game</button>
  </form>
</div>

<style>
  .form-container {
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
  }

  .form-group {
    margin-bottom: 20px;
  }

  .form-control {
    width: 100%;
    padding: 8px;
    border: 2px solid #00ff00;
    background: rgba(0, 0, 0, 0.8);
    color: #00ff00;
    font-family: "Press Start 2P", cursive;
    font-size: 12px;
  }

  textarea.form-control {
    height: 100px;
    resize: vertical;
  }

  small {
    display: block;
    margin-top: 5px;
    color: #666;
    font-size: 10px;
  }

  .btn-primary {
    background: #00ff00;
    color: black;
    border: none;
    padding: 10px 20px;
    cursor: pointer;
    font-family: "Press Start 2P", cursive;
    font-size: 14px;
  }

  .btn-primary:hover {
    background: #00cc00;
  }
</style>
